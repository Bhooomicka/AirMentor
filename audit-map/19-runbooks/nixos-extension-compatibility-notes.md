# NixOS Extension Compatibility Notes

- Codex CLI is available from the VS Code OpenAI extension bundle.
- Arctic works from the user npm prefix on this machine.
- Playwright is already covered by the Nix shell.
- Repo-local Caveman auto-hooks are not configured because the repo-root `.codex` path is currently a file conflict.
