# Root Image Folder Status

This folder is reserved for shared image artifacts that do not belong to a more specific audit domain.

It is currently empty.

That is acceptable today because the audit evidence is primarily text, code, logs, status files, and live HTML artifacts under:

- `audit-map/17-artifacts/screenshots/`
- `audit-map/17-artifacts/live/`
- `audit-map/17-artifacts/diffs/`

If screenshots or diagrams are produced later and do not belong inside a domain-specific `image/` folder, place them here and cross-link them from the owning report.
