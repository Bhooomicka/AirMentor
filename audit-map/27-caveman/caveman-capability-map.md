# Caveman Capability Map

Verified locally:

- global install through `npx skills add JuliusBrussee/caveman -g -a codex -y --copy`
- installed skills:
  - `caveman`
  - `caveman-compress`
  - `caveman-commit`
  - `caveman-help`
  - `caveman-review`

Verified from project README:

- token-saving terse response mode
- memory compression helper
- repo-local Codex auto-activation path through `.codex/hooks.json`

Local limitation:

- repo-local auto-activation path is blocked by the existing repo-root `.codex` file
