This directory is reserved for project-scoped Arctic configuration.

No provider-specific project config was written during bootstrap because the installed Arctic build still needs authenticated providers and verified provider identifiers for this machine.

Use the runbooks in `audit-map/19-runbooks/` first, then add project config here only after the first successful authenticated Arctic session is recorded.
