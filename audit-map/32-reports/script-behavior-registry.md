# Script Behavior Registry

Pass: `script-behavior-pass`
Context: `bootstrap`
Date: `2026-04-16T14:27:32Z`
Model/provider/account: `gpt-5.4-mini` / `native-codex` / `native-codex-session`
Caveman used: `no`
Live verification performed in this pass: `no`

## Environment Drift Check

- At pass start, the control plane still reported `script-behavior-pass` as `running` in [status](/home/raed/projects/air-mentor-ui/audit-map/29-status/audit-air-mentor-ui-bootstrap-script-behavior-pass.status) and [operator-dashboard.md](/home/raed/projects/air-mentor-ui/audit-map/32-reports/operator-dashboard.md), while the existing log stream contained wrapper progress rather than durable audit content.
- The current pass writes the missing durable registry artifact plus the pass-scoped last-message file, then reconciles the control plane to terminal completion.
- `same-student-cross-surface-parity-pass` remains blocked on durable parity evidence and should stay non-creditable until it writes an explicit parity artifact.

## Registry

| Script family | Invocation path(s) | Behavior | Inputs / assumptions | Files / services touched | Semantic guarantee | False-confidence risk |
| --- | --- | --- | --- | --- | --- | --- |
| Credential resolution and password probing | `scripts/system-admin-live-auth.mjs`, `scripts/teaching-password-resolution.mjs` | Pure env resolution for live admin credentials and a candidate-password probe against `POST /api/session/login`. | Live credential env vars, app/API URLs, candidate passwords, origin header. | No repo writes; network to the live or local session API only. | `system-admin-live-auth.mjs` is read-only; `teaching-password-resolution.mjs` is read-only with respect to product data but still exercises the live auth surface. | A returned password or 200 response proves only the session contract for that credential pair, not full role parity, deployed correctness, or credential safety. |
| Live admin wrappers and Playwright smokes | `scripts/playwright-admin-live-acceptance.sh`, `scripts/playwright-admin-live-request-flow.sh`, `scripts/playwright-admin-live-teaching-parity.sh`, `scripts/playwright-admin-live-session-security.sh`, `scripts/playwright-admin-live-accessibility-regression.sh`, `scripts/playwright-admin-live-keyboard-regression.sh`, `scripts/playwright-admin-live-proof-risk-smoke.sh`, `scripts/live-admin-common.sh`, `scripts/playwright-browser-common.sh`, `scripts/playwright-smoke.sh`, `scripts/playwright-firefox-acceptance.sh`, `scripts/firefox-acceptance.mjs`, `scripts/system-admin-live-acceptance.mjs`, `scripts/system-admin-live-request-flow.mjs`, `scripts/system-admin-live-session-security.mjs`, `scripts/system-admin-live-accessibility-regression.mjs`, `scripts/system-admin-live-keyboard-regression.mjs`, `scripts/system-admin-proof-risk-smoke.mjs`, `scripts/system-admin-teaching-parity-smoke.mjs` | Dual-mode wrappers either start a seeded local backend + preview stack or, when `AIRMENTOR_LIVE_STACK=1`, wait on the real Pages + Railway pair and run credentialed Playwright flows. The flows cover acceptance, request advancement, teaching parity, session-security, accessibility, keyboard, and proof-risk / semester-walk behavior. | `AIRMENTOR_LIVE_STACK`, `PLAYWRIGHT_APP_URL`, `PLAYWRIGHT_API_URL`, live admin credentials, Playwright browser paths, output directory, optional proof semester targets. | Writes screenshots, JSON reports, lock files, preview logs, semester-walk summaries, and prefixed artifact copies; live proof-risk and teaching-parity smokes mutate proof lifecycle, semester activation, faculty profile, appointment state, and request closeout when they succeed. | Browser-flow verification is real, but the guarantee is still only as strong as the stack mode and the captured artifacts. | The same script names can run against seeded local data and look green without proving deployed semantic truth. Several flows are mutating by design: `reset workspace`, proof import/approval, semester activation, faculty profile patching, request closeout, logout/relogin, and role switching. |
| Deploy readiness and recovery orchestration | `scripts/check-railway-deploy-readiness.mjs`, `scripts/verify-final-closeout-live.sh`, `scripts/verify-final-closeout.sh`, `scripts/dev-live.sh`, `scripts/railway-recovery-chain.sh` | Checks Railway variables, session contract, boot smoke, deploy-path smoke, and live health; then orchestrates live closeout, local closeout, or Railway recovery runs. | Railway CLI/auth, GitHub CLI, live URLs, live credentials, optional sync-mode env vars, embedded Postgres for deploy-path smoke. | May set Railway vars in sync mode; writes JSON diagnostics under `output/`; starts embedded Postgres for boot/deploy smoke; triggers GitHub Actions deploy workflow in the recovery chain. | Configuration and orchestration truth only. | A green preflight or recovery run does not prove deployed browser semantics. The same wrapper names can represent local seeded verification or live verification depending on env, and recovery can advance closeout even when source artifacts are stale. |
| Seeded runtime, parity fixture, and test harness | `air-mentor-api/scripts/generate-academic-parity-seed.ts`, `air-mentor-api/scripts/start-seeded-server.ts`, `air-mentor-api/scripts/run-vitest-suite.mjs`, `air-mentor-api/scripts/evaluate-proof-risk-model.ts`, `air-mentor-api/scripts/curriculum_linkage_nlp.py`, `scripts/proof-risk-semester-walk.mjs`, `scripts/proof-risk-semester-walk-probe.mjs`, `scripts/report-compat-route-callers.mjs` | Generates the deterministic same-student parity seed, boots the seeded API, runs Vitest file-by-file, evaluates proof-risk artifacts, walks semesters, and inventories route callers. `curriculum_linkage_nlp.py` is a stdin/stdout NLP helper with deterministic, spaCy, embedding, and optional Ollama branches. | Source data from `src/data.ts`, embedded Postgres, test DB wiring, `AIRMENTOR_BACKEND_SUITE`, optional `spacy`, `sentence-transformers`, and Ollama env/config. | Writes `air-mentor-api/src/db/seeds/platform.seed.json`, seeded-server readiness payloads, per-file Vitest output, proof-risk JSON/Markdown reports, semester-walk summaries, and route inventory output. | Fixture generation and local evaluation are deterministic within their data contracts. `curriculum_linkage_nlp.py` is read-only on disk and only emits JSON to stdout. | `run-vitest-suite.mjs` excludes proof-rc files unless `AIRMENTOR_BACKEND_SUITE=proof-rc`, so a green default run can hide proof-rc gaps. `generate-academic-parity-seed.ts` proves fixture intent, not live consumer freshness. `curriculum_linkage_nlp.py` can degrade or fall back while still producing output, so deployment dependency posture must be verified separately. |
| Detached execution and evidence promotion | `scripts/run-detached.sh`, `scripts/finalize-stage-02-after-session.sh`, `scripts/finalize-stage-07b-after-session.sh`, `scripts/finalize-stage-07c-after-session.sh`, `scripts/finalize-stage-08a-after-session.sh`, `scripts/finalize-stage-08b-after-session.sh`, `scripts/finalize-stage-08c-after-session.sh`, `scripts/closeout-stage-02-success.mjs`, `scripts/closeout-stage-07b-success.mjs`, `scripts/closeout-stage-07c-success.mjs`, `scripts/closeout-stage-08a-success.mjs`, `scripts/closeout-stage-08b-success.mjs`, `scripts/closeout-stage-08c-success.mjs`, `scripts/snapshot-final-closeout-artifacts.mjs` | Launches detached jobs via tmux or nohup, waits for log completion, then promotes stage-scoped evidence bundles into ledgers, manifests, indexes, defect registers, assertion matrices, coverage matrices, and closeout docs. | Detached session name + log path, stage-scoped artifacts, live/local closeout bundles, tmux/nohup availability, prior ledger/manifold state. | Writes wrapper scripts, log/pid files, stage bundle JSON, and the closeout corpus under `output/playwright/` and `docs/closeout/`; may also copy prefixed artifacts into promotion bundles. | This is evidence-governance and stage-gate bookkeeping, not product truth. | Green closeout scripts can promote stale artifacts or historical bundles if the upstream source set is old. Log-based success detection can overstate confidence when the underlying semantic truth has drifted. |

## Other Important Helpers

- `scripts/report-compat-route-callers.mjs` is a read-only repository inventory helper. It can assert route-caller cleanliness, but it does not prove live deployment behavior.
- `scripts/firefox-acceptance.mjs` and `scripts/playwright-firefox-acceptance.sh` are local-browser smoke helpers. They prove only the local preview path and can reset the seeded workspace when the reset control is visible.
- `scripts/playwright-browser-common.sh` is bootstrap glue for Playwright browser path discovery. It is environment plumbing, not a semantic verifier.

## Residual Blockers

- No safe read-only live proof/parity observer exists yet. The proof smoke and teaching-parity smoke are both mutating helpers.
- Live authenticated admin, teacher, HoD, mentor, and student flows are still not directly observed in a credentialed live environment from this shell.
- Deployed proof-risk freshness and fallback frequency still require live verification.
- The current deployment dependency posture for Python NLP, `spacy`, `sentence-transformers`, and optional Ollama remains an environment-sensitive runtime question, not a proven deploy-time fact.
- Seed-data-to-live parity remains unresolved even after the parity seed generator is now durably mapped.
- Evidence promotion scripts still need separate freshness checks before they can be trusted as closeout proof.

## Pass Output Contract

- Covered: live auth helpers, live wrappers, deploy-readiness orchestration, seeded runtime / parity fixture generation, proof-risk evaluation harnesses, detached execution, and evidence-promotion scripts.
- Files updated: [script-behavior-registry.md](/home/raed/projects/air-mentor-ui/audit-map/32-reports/script-behavior-registry.md), [audit-air-mentor-ui-bootstrap-script-behavior-pass.last-message.md](/home/raed/projects/air-mentor-ui/audit-map/32-reports/audit-air-mentor-ui-bootstrap-script-behavior-pass.last-message.md), status/checkpoint files, coverage ledgers, review-status, memory, reconciliation, operator dashboard, dependency graph, data-flow map, and test-gap ledger.
- Remains uncovered: safe read-only live proof/parity observation, direct credentialed live role flows, proof-risk freshness on deployed infrastructure, deployment dependency posture for NLP helpers, and seed-data-to-live parity.
- Contradictions found: `C-013` is resolved by this pass; the earlier missing durable artifact / missing last-message claim is now historical drift only.
- Risks discovered: local seeded execution can masquerade as live success; closeout promotion can carry stale artifacts forward; proof-rc can be hidden by default Vitest routing; mutating live smokes can change the truth they are supposed to observe.
- Routing/provider/account changed: no.
- Caveman used: no.
- Live verification performed: no.
- Next pass: `backend-provenance-pass` continuation, then `same-student-cross-surface-parity-pass`.
- Manual checkpoint required: no.
